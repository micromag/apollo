enum Variant {
    Primary,
    Secondary
}

enum Size {
    xs,
    s,
    md,
    lg,
    xl
}

enum Icon {
    cog
}

enum BoxType {
    Checkbox,
    Radio
}

enum InputType {
    text,
    number,
    email,
    password,
    date,
    time,
    color,
    search,
    url,
    phone, 
    range
}

export { Variant, Size, Icon, InputType, BoxType };